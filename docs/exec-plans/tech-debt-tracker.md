# Tech debt tracker

Known debt in the **meta-repo** (not in submodules — that lives in each submodule's own
tracker). When you encounter debt, append a row. When you pay it down, remove the row in the
same PR.

| Item | Opened | Notes |
|---|---|---|
| Upstream naming cleanup — retire "bridge" and "BYOC" across all submodules | 2026-04-28 | Active plan with per-submodule counts and suggested rename order: [`exec-plans/active/0001-upstream-naming-cleanup.md`](active/0001-upstream-naming-cleanup.md). Largest impact: `livepeer-openai-gateway` (1699 `bridge` hits, mostly cascading from `bridge-ui/` directory and `eslint-plugin-livepeer-bridge`). |
| `--verify` should crosscheck `livepeer-up-installer/templates.lock.json` SHAs against this meta-repo's submodule pins where they overlap | 2026-04-28 | Two layers of pinning today (meta-repo's `.gitmodules` + installer's `templates.lock.json`). Coherence is operator-checked, not enforced. Add a script extension once 2+ overlapping submodules are in. See `suite-architecture.md` "Cross-pinning relationship". |
| Two submodules expect sibling at `../livepeer-modules-project/`, we have it at `../livepeer-modules/` | 2026-04-28 | Affects `livepeer-secure-orch-console` and `livepeer-gateway-console` — both run `npm run proto:gen` against the relative sibling path. `livepeer-orch-coordinator` documents the same path but doesn't generate protos at runtime. Options: (a) symlink `livepeer-modules-project -> livepeer-modules` at meta-repo root, (b) patch each console's proto:gen path, (c) rename the submodule path to `livepeer-modules-project` to match upstream. Option (c) is least invasive across consumers; pick before any contributor runs `npm run proto:gen` from inside the suite checkout. |
| `livepeer-gateway-console` has no release tag yet | 2026-04-28 | Pinned to `main` HEAD `1bf67d9`. Plan-0001 MVP just landed (commit message "plan 0001: capture decisions log"). Tag `v0.1.0` upstream and repin before the next suite release. |
| `openai-worker-node` README claims "no source code yet" but repo has ~290 KB of Go | 2026-04-28 | The post-scaffold state landed without a README refresh. Open a doc-gardening PR upstream to update status from "Scaffolding" to whatever the v1.1.3 capabilities reflect. Cross-check what's actually wired vs the listed capability set. |
| Payments proto duplicated between `openai-worker-node` and `livepeer-modules/payment-daemon` | 2026-04-28 | Same `.proto` files vendored on both sides. Drift risk grows with proto file count. Now `livepeer-openai-gateway` is also in the suite as a third consumer — promote the protos to a shared submodule or the `chain-commons` lib before they fan out further. |
| Daemon image-version drift across submodule READMEs | 2026-04-28 | Three different anchors today for the same image namespace: `livepeer-modules` README publishes daemons at `v1.0.0`; `livepeer-openai-gateway` README's compose pins them at `v1.4.0`; user memory had `v0.8.10` (which actually applies to `tztcloud/livepeer-openai-gateway` itself, confirmed in this session). Pick one source of truth — likely the daemon images in `livepeer-modules` — and have other submodules reference it rather than hard-coding. |
| `--verify` should crosscheck `livepeer-openai-gateway/package-lock.json` against the `livepeer-openai-gateway-core` submodule pin | 2026-04-28 | Both repos are now submodules. The shell consumes the engine via npm (`@cloudspe/livepeer-openai-gateway-core`, currently `0.2.0`); the engine submodule pin should match the npm version exactly. Add a check that parses the lockfile and asserts the version equals the engine's pinned tag. Pre-1.0 versioning makes this especially load-bearing — a minor bump can ship breaking changes. |
| Naming drift: `livepeer-payment-library` (legacy) vs `payment-daemon` (current) vs `PayerDaemon` (gRPC service name) | 2026-04-28 | All three names refer to the same component. `livepeer-openai-gateway`'s README and code still say "PayerDaemon"; `livepeer-modules` calls it "payment-daemon"; `livepeer-payment-library` is a legacy alias still mentioned upstream. Roll into the broader naming cleanup ([`exec-plans/active/0001-upstream-naming-cleanup.md`](active/0001-upstream-naming-cleanup.md)). |
| `livepeer-openai-gateway` has no release tag, only milestone tags | 2026-04-28 | Tags upstream are `PRE_OSS_REFACTOR` and `PHASE1_SCAFFOLD` — neither is a version. Pinned to `main` HEAD `65a1002`. The deployed image (`tztcloud/livepeer-openai-gateway:v0.8.10`) implies a release flow exists but without git tags. Tag the next stable upstream and repin before the next suite release. |
| `livepeer-video-platform/apps/transcode-worker-node/` has license TBD | 2026-04-28 | Engine (`livepeer-video-core`) is MIT; shell is proprietary; worker is unresolved per the README's components table. Pick a license — affects whether operators can fork the worker. |
| `livepeer-video-platform` should be split into separate shell + worker repos | 2026-04-28 | OpenAI and VTuber workloads ship shell + worker as separate repos (`livepeer-<workload>-gateway` + `<workload>-worker-node`); only video bundles them in a monorepo. User wants the split-repo model to be the suite-wide convention. Plan: extract `apps/api/` → `livepeer-video-gateway` repo, extract `apps/transcode-worker-node/` → `video-worker-node` repo (note worker drops the `livepeer-` prefix per convention), retire `livepeer-video-platform` once both extractions land. Repin three submodules (engine pin unchanged; new gateway + new worker pins replace the platform pin). Tracked in [`exec-plans/active/0002-suite-wide-alignment.md`](active/0002-suite-wide-alignment.md). |
| `service-registry-daemon` publisher mode runs on workers, contradicting `livepeer-modules`'s host-archetype model | 2026-04-28 | Originally flagged on `livepeer-video-platform`; now confirmed real (not a typo) since `vtuber-worker-node` does the same thing. Likely a leaf-vs-rooted manifest separation (worker publisher signs its own capability fragment; secure-orch publisher signs the rooted manifest pointing at workers). But this isn't documented anywhere in the suite. Action: read the publisher's gRPC surface and either update `livepeer-modules`'s host-archetype docs to acknowledge the divergence, or document it as a per-workload deployment variation. |
| `livepeer-vtuber-project` license is unspecified | 2026-04-28 | Engine repos in suite are MIT; shells are proprietary; this consumer SaaS is "TBD before first external release" per the README. It's likely closed (it's a product) but not yet stated. Pick a license before any external/contractor access. |
| `livepeer-vtuber-project` is mid-realignment | 2026-04-28 | README says "architecture realignment in flight"; phase 1 partially landed. The `livepeer-vtuber-gateway` sibling repo has been created since this entry was opened; `vtuber-worker-node` is still announced-but-not-existing. Pinning to `main` HEAD `b1bcdac` for now — expect churn. Cut a tag and repin once realignment stabilizes. |
| VTuber pair is lopsided: gateway at M9, worker at M1 | 2026-04-28 | `livepeer-vtuber-gateway` is M9-complete (348 tests passing); `vtuber-worker-node` is M1-skeleton (intentionally refuses to start). The gateway's M9 smoke test still has no real target. Track the worker's M2-M4 progress; once contract tests + module impl land, repin both and run the smoke end-to-end. |
| `livepeer-vtuber-gateway` license is TBD | 2026-04-28 | Same situation as `livepeer-vtuber-project` — "TBD before first external release." Pick a license. |
| `livepeer-vtuber-gateway` has no release tag | 2026-04-28 | Pinned to `main` HEAD `7138f39`. M9 means MVP is fully assembled — natural place to cut `v0.1.0` upstream and repin. |
| `vtuber-worker-node` has no release tag | 2026-04-28 | Pinned to `main` HEAD `52a0336`. Tag once M2-M4 land (StreamingModule interface + vtuber-session module impl + contract tests). |
| `vtuber-worker-node` license is TBD | 2026-04-28 | Same as the gateway and `livepeer-vtuber-project`. Pick a license. |
